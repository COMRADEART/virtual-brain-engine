{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Computer Brain",
  "windows": ["main", "pet"],
  "permissions": [
    "core:default",
    "core:window:default",
    "shell:default"
  ]
}
